export const metadata = {
  title: "Sign in",
  description: "Sign in to CAT (Content Analysis Toolkit) to review media forensics and analysis results.",
};

export default function PublicLayout({ children }) {
  return children;
}
